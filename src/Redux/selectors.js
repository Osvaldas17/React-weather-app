const LocalAdministrationList = ({ localAdministrations }) => localAdministrations
const allLocations = ({allLocations}) => allLocations
const topCities = ({topCities}) => topCities
const citiesInsideDivision = ({citiesInsideDivision}) => citiesInsideDivision

const selectors = {
    LocalAdministrationList,
    allLocations,
    topCities,
    citiesInsideDivision,
}

export default  selectors